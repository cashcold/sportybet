import { Match } from '../types';

/**
 * Real fixtures are populated dynamically from The Odds API and official live feeds.
 * Static mock overrides are disabled to prevent fake match simulations.
 */
export const REAL_UPCOMING_FIXTURES: Match[] = [];
